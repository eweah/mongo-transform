  // ======== Examples ============
 // const ${option.model.toLowerCase()} = new ${option.model};


 // ${option.model.toLowerCase()}.create({property: 'value', anotherProperty: 'another value'})
 // ${option.model.toLowerCase()}.once('create', console.log);
 // ${option.model.toLowerCase()}.once('create-error', console.error);

 // ${option.model.toLowerCase()}.all();
 // ${option.model.toLowerCase()}.once('all', console.log);
 // ${option.model.toLowerCase()}.once('all-error', console.error);